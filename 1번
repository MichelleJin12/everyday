#include <iostream>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(0);


	int N, M, sum = 0;
	cin >> N >> M;
	for (int i = 1; M * i <= N; i++) {
		sum += M * i;
	}
	cout << sum;

}
