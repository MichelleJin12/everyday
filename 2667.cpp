//단지 번호 붙이기

#include <iostream>
#include <vector>
#define APT_MAX 26
std::vector<bool> v[APT_MAX];
int apt[APT_MAX][APT_MAX];
int main()
{
	int a;
	std::cin >> a;
	for (int i = 0; i < a; i++)
	{
		{
			int b;
			apt[i][j];
		}
	}

	return 0;
}